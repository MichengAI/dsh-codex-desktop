# 更新日志

[English](CHANGELOG.md)

以下记录最近发布的五个版本。

## 1.0.75 — 2026-09-24

- 离线升级旧 Profile 时会并入本机原有的 pnpm 元数据缓存，旧 lockfile 不联网也能通过校验。1.0.41 的 lockfile 引用了随包仓库已不再收录的 cosmokit 1.8.1。
- 升级冒烟不再按 id 比对补丁条目。1.0.41 的 Profile 把桌面桥接写成没有 id 的 insert 列表，而产品自身的桥梁迁移会按设计移除这条声明。

## 1.0.74 — 2026-09-24

- 合并旧 Profile 时一并并入本机原有的 pnpm 元数据缓存，离线升级才能校验旧 lockfile。1.0.73 的 Windows 1.0.41 升级校验会失败：那份 lockfile 引用了随包仓库已不再收录的包，例如 cosmokit 1.8.1。

## 1.0.73 — 2026-09-24

- 升级冒烟改为从 Profile 补丁读取 PUA 默认值：0.1.7 起全局插件配置写在补丁条目里，`settings.yaml` 已废弃不再写入，1.0.72 因此在 Windows 的 1.0.51 升级校验上因文件不存在中断。
- 打包冒烟的本机 HTTP 首次请求加入重试。装配 Profile 会重启本机服务，重置换连接以前会直接判失败（macOS Intel 就是这样挂的）。

## 1.0.72 — 2026-09-24

- 离线仓库先复制成不含链接的目录再打包，并按 Windows 启动时的解压复制再做一次离线安装。1.0.71 只校验了打包前的目录，安装包里仍可能丢掉 lucide-react 1.48.0。

## 1.0.71 — 2026-09-24

- 接受 0.1.7 的 bundle 补丁列表。1.0.70 把 `@deepseek-ai/dsh-web-app` 判成缺失，因为它的 patch 字段是数组，五个平台都在 HTTP 服务起来前退出。

